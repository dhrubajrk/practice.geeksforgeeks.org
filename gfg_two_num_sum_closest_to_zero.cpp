#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,min,a,b,i,j;

    cin>>t;
    while(t--)
    {
        cin>>n;
        int arr[n];
        for(int i=0;i<n;++i)
            cin>>arr[i];
        sort(arr,arr+n);
        i=0;
        j=n-1;
        if(arr[i])
    }
}
